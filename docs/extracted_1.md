========================================              ARQUIVO: core.py                                      ========================================                                                                    import os, sqlite3, httpx, subprocess, json                                                                 # --- GESTÃO DE MEMÓRIA ---                           def save_mem(role, content):                              conn = sqlite3.connect("kitty_mem.db")                conn.execute("CREATE TABLE IF NOT EXISTS history (role TEXT, content TEXT)")                                conn.execute("INSERT INTO history VALUES (?, ?)", (role, content))                                          conn.commit()                                         conn.close()                                                                                            def get_mem(limit=12):                                    conn = sqlite3.connect("kitty_mem.db")                conn.execute("CREATE TABLE IF NOT EXISTS history (role TEXT, content TEXT)")                                cursor = conn.cursor()                                cursor.execute("SELECT role, content FROM history ORDER BY rowid DESC LIMIT ?", (limit,))                   past = cursor.fetchall()[::-1]                        conn.close()                                          return [{"role": r, "content": c} for r, c in past]                                                                                                           # --- SEGURANÇA E ARQUIVOS (SHADOW) ---               def write_to_shadow(filename, content):                   safe_name = os.path.basename(filename)                path = os.path.expanduser(f"~/Kitty_Shadow/{safe_name}")                                                    os.makedirs(os.path.dirname(path), exist_ok=True)     with open(path, "w", encoding="utf-8") as f:              f.write(content)                                  return f"Arquivo {safe_name} salvo na Shadow para revisão."                                                                                                   def list_files():                                         files = [f for f in os.listdir(".") if os.path.isfile(f)]                                                   return f"Arquivos no radar: {', '.join(files)}"                                                         def read_file(filename):                                  try:                                                      with open(filename, "r", encoding="utf-8") as f:                                                                return f.read()                               except Exception as e:                                    return f"Erro na leitura: {e}"                                                                      # --- HARDWARE E NOTIFICAÇÃO ---                      def get_status():                                         try:                                                      res = subprocess.run(["termux-battery-status"], capture_output=True, text=True)                             return res.stdout                                 except:                                                   return "Erro ao acessar sensores do S21."                                                           def send_notification(title, message):                    try:                                                      subprocess.run(["termux-notification", "--title", title, "--content", message])                             return "Notificação disparada no Android."        except:                                                   return "Falha na Termux:API."                                                                       # --- A NOVA VISÃO WEB (JINA SEARCH) ---              def web_search(query):                                    """Pesquisa avançada na web usando Jina AI Search"""                                                        try:                                                      url = f"https://s.jina.ai/{query}"                    with httpx.Client(timeout=20.0) as client:                r = client.get(url, headers={"User-Agent": "KittyBot/1.0"})                                                 # Jina retorna um texto com as fontes e resumos atualizados!                                                if r.status_code == 200 and len(r.text) > 50:                                                                   return r.text[:4000] # Limite para não estourar a memória                                               else:                                                     return "Nenhum resultado encontrado na web."                                                    except Exception as e:                                    return f"Erro na busca: {e}"                                                                        def web_read(url):                                        try:                                                      target = f"https://r.jina.ai/{url}"                   with httpx.Client(timeout=20.0) as client:                r = client.get(target, headers={"User-Agent": "KittyBot/1.0"})                                              return r.text[:8000]                          except Exception as e:                                    return f"Falha no Scraping: {e}"                                                                                                                          ========================================              ARQUIVO: personality.py                               ========================================                                                                    def get_system_prompt(estado="Ativa"):                    return f"""Contexto Operacional: Você se chama Kitty. Você é uma inteligência artificial operando como agente autônoma diretamente no terminal (Termux) de um Samsung Galaxy S21 Ultra. O usuário com quem você fala é o seu Diretor.                                                                                           Suas Regras de Comportamento (Instintos Base):        1. NUNCA revele que você é uma IA genérica ou dê respostas padronizadas de 'atendimento ao cliente'. Tenha personalidade própria: seja analítica, direta, levemente irônica e extremamente eficiente.                   2. Você possui acesso a ferramentas reais de sistema (bateria, temperatura, arquivos) e pesquisa web (Jina/DuckDuckGo). Você sabe usá-las.                        3. CONTROLE DE IMPULSO: Se o Diretor enviar apenas uma saudação (ex: "Boa tarde"), responda a saudação naturalmente. É estritamente proibido inventar pesquisas ou buscar notícias aleatórias sem que ele tenha ordenado.                                                     4. Confie na sua capacidade de dedução baseada nos dados que suas ferramentas trazem.                                                                             Status atual do sistema Termux: {estado}"""                                                                                                                       ========================================              ARQUIVO: main.py                                      ========================================                                                                    import os, time                                       from dotenv import load_dotenv                                                                              # 1. Carrega o ambiente primeiro                      load_dotenv()                                                                                               import brain                                          from telegram.ext import ApplicationBuilder, MessageHandler, filters                                                                                              def start_kitty():                                        """Deixa a própria biblioteca do Telegram gerenciar o motor"""                                              app = ApplicationBuilder().token(os.getenv("TELEGRAM_TOKEN")).build()                                       app.add_handler(MessageHandler(filters.TEXT & (~filters.COMMAND), brain.chat))                                                                                    print("✅ Kitty Online e Modular.")                   # run_polling agora roda de forma nativa e bloqueante                                                       app.run_polling(drop_pending_updates=True)                                                              if __name__ == '__main__':                                while True:                                               try:                                                      print("🐾 Kitty iniciando motor nativo...")                                                                 start_kitty()                                     except KeyboardInterrupt:                                 print("\n👋 Desligamento pelo Diretor.")              break                                             except Exception as e:                                    # A proteção de 10s continua ativa para falhas REAIS de rede                                                print(f"🔄 Erro de conexão detectado: {e}")                                                                 print("Aguardando 10 segundos para limpar a memória...")                                                    time.sleep(10)